<?xml version="1.0" encoding="utf-8"?>
<!--
  Solo builds DEBUG: permite HTTP en claro para probar contra un servidor
  HubPlay de desarrollo sin TLS (emulador → 10.0.2.2:8097, TV box → IP de
  la LAN). El manifest principal deja usesCleartextTraffic=false para
  release: en producción el servidor va detrás de TLS (nginx / TOFU pin).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic" />

</manifest>
