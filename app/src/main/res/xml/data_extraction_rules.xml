<?xml version="1.0" encoding="utf-8"?>
<!-- Disallow auto-backup of credentials. Tokens live in DataStore which
     IS a backup target by default; we exclude the file explicitly so a
     user restoring on a new device has to log in again. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="file" path="datastore/" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="datastore/" />
    </device-transfer>
</data-extraction-rules>
